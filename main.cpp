#include <iostream>
#include <chrono>
#include <fstream>
#include "LinkedList.h"
#include "AdjacencyList.h"
#include "RandomGen.h"

using namespace std;

int main()
{

    // AdjacencyList g1(5);
    // g1.addEdge(0,2);
    // g1.addEdge(1,2);
    // g1.addEdge(2,3);
    // g1.addEdge(2,4);
    // g1.addEdge(3,4);
    // g1.genDegreeList();
    // g1.save("graph2.txt");

    // g1.colorGraph(AdjacencyList::Coloring::SLVO);

    // AdjacencyList g1("graph.txt");
    // g1.print();
    // g1.colorGraph(AdjacencyList::Coloring::SLVO);
    

    // AdjacencyList g2("graph2.txt");
    // g2.print();
    // g2.colorGraph(AdjacencyList::Coloring::LLVO);

    //AdjacencyList g3("graph.txt");
    //g3.print();

    return 0;

}